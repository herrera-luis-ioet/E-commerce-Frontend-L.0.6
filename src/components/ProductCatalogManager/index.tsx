import ProductCatalogManager from '../../features/products/ProductCatalogManager';

/**
 * PUBLIC_INTERFACE
 * Re-export the ProductCatalogManager component from features/products
 * This ensures backward compatibility if any code is importing from this path
 */
export default ProductCatalogManager;
